{% extends 'base.html.twig' %}

{% block title %}Formulation{% endblock %}

{% block body %}
    <h1>Formulation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formulation.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ formulation.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_formulation_index') }}">back to list</a>

    <a href="{{ path('admin_formulation_edit', {'id': formulation.id}) }}">edit</a>

    {{ include('Admin/admin_formulation/_delete_form.html.twig') }}
{% endblock %}
