{% extends 'adminBase.html.twig' %}

{% block title %}Formulation{% endblock %}

{% block body %}
<div class="row d-flex">
    <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb" class="d-flex justify-content-end">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('admin') }}">Admin</a></li>
            <li class="breadcrumb-item"><a href="{{ path('admin_formulation_index') }}">Formulations</a></li>
            <li class="breadcrumb-item active" aria-current="page">Détails</li>
        </ol>
    </nav>
</div>
    <h1>Détails formulation</h1>
    <a href="{{ path('admin_formulation_index') }}"><button class="btn btn-outline-info px-4"><i class="fas fa-undo-alt"></i></button></a>

    <a href="{{ path('admin_formulation_edit', {'id': formulation.id}) }}"><button class="btn btn-outline-warning px-4 mx-2"><i class="far fa-edit"></i></button></a>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ formulation.id }}</td>
            </tr>
            <tr>
                <th>Grammaire</th>
                <td>{{ formulation.meaning.grammar.title }}</td>
            </tr>
            <tr>
                <th>Signification</th>
                <td>{{ formulation.meaning.content }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ formulation.content }}</td>
            </tr>
        </tbody>
    </table>


    {{ include('Admin/admin_formulation/_delete_form.html.twig') }}
{% endblock %}
