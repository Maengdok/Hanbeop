{% extends 'base.html.twig' %}

{% block title %}Answer{% endblock %}

{% block body %}
    <h1>Answer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ answer.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ answer.content }}</td>
            </tr>
            <tr>
                <th>IsCorrect</th>
                <td>{{ answer.isCorrect ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_answer_index') }}">back to list</a>

    <a href="{{ path('admin_answer_edit', {'id': answer.id}) }}">edit</a>

    {{ include('Admin/admin_answer/_delete_form.html.twig') }}
{% endblock %}
