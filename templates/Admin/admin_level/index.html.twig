{% extends 'base.html.twig' %}

{% block title %}Level index{% endblock %}

{% block body %}
    <h1>Level index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for level in levels %}
            <tr>
                <td>{{ level.id }}</td>
                <td>{{ level.name }}</td>
                <td>
                    <a href="{{ path('admin_level_show', {'id': level.id}) }}">show</a>
                    <a href="{{ path('admin_level_edit', {'id': level.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_level_new') }}">Create new</a>
{% endblock %}
