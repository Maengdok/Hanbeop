{% extends 'base.html.twig' %}

{% block title %}Level{% endblock %}

{% block body %}
    <h1>Level</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_level_index') }}">back to list</a>

    <a href="{{ path('admin_level_edit', {'id': level.id}) }}">edit</a>

    {{ include('Admin/admin_level/_delete_form.html.twig') }}
{% endblock %}
