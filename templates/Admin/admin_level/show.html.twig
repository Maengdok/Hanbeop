{% extends 'adminBase.html.twig' %}

{% block title %}Level{% endblock %}

{% block body %}
<div class="row d-flex">
    <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb" class="d-flex justify-content-end">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('admin') }}">Admin</a></li>
            <li class="breadcrumb-item"><a href="{{ path('admin_level_index') }}">Niveaux</a></li>
            <li class="breadcrumb-item active" aria-current="page">Détails</li>
        </ol>
    </nav>
</div>
    <h1>Détails niveau</h1>
    <div class="col d-flex d-inline">
        <a href="{{ path('admin_level_index') }}"><button class="btn btn-outline-info px-4"><i class="fas fa-undo-alt"></i></button></a>

        <a href="{{ path('admin_level_edit', {'id': level.id}) }}"><button class="btn btn-outline-warning px-4 mx-2"><i class="far fa-edit"></i></button></a>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ level.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ level.name }}</td>
            </tr>
        </tbody>
    </table>


    {{ include('Admin/admin_level/_delete_form.html.twig') }}
{% endblock %}
