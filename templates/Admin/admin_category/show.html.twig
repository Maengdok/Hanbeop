{% extends 'adminBase.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="row d-flex">
    <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb" class="d-flex justify-content-end">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('admin') }}">Admin</a></li>
            <li class="breadcrumb-item"><a href="{{ path('admin_category_index') }}">Categories</a></li>
            <li class="breadcrumb-item active" aria-current="page">Détails</li>
        </ol>
    </nav>
</div>
    <h1>Détails catégorie</h1>

    <a href="{{ path('admin_category_index') }}"><button class="btn btn-outline-info px-4"><i class="fas fa-undo-alt"></i></button></a>

    <a href="{{ path('admin_category_edit', {'id': category.id}) }}"><button class="btn btn-outline-warning px-4"><i class="far fa-edit"></i></button></a>
    <table class="table table-hover">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>


    {{ include('Admin/admin_category/_delete_form.html.twig') }}
{% endblock %}
