{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ question.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_question_index') }}">back to list</a>

    <a href="{{ path('admin_question_edit', {'id': question.id}) }}">edit</a>

    {{ include('Admin/admin_question/_delete_form.html.twig') }}
{% endblock %}
