{% extends 'base.html.twig' %}

{% block title %}UserAnswer{% endblock %}

{% block body %}
    <h1>UserAnswer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_answer.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_user_answer_index') }}">back to list</a>

    <a href="{{ path('admin_user_answer_edit', {'id': user_answer.id}) }}">edit</a>

    {{ include('Admin/admin_user_answer/_delete_form.html.twig') }}
{% endblock %}
