{% extends 'base.html.twig' %}

{% block title %}Grammar{% endblock %}

{% block body %}
    <h1>Grammar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grammar.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ grammar.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_grammar_index') }}">back to list</a>

    <a href="{{ path('admin_grammar_edit', {'id': grammar.id}) }}">edit</a>

    {{ include('Admin/admin_grammar/_delete_form.html.twig') }}
{% endblock %}
