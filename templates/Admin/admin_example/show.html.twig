{% extends 'base.html.twig' %}

{% block title %}Example{% endblock %}

{% block body %}
    <h1>Example</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ example.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ example.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_example_index') }}">back to list</a>

    <a href="{{ path('admin_example_edit', {'id': example.id}) }}">edit</a>

    {{ include('Admin/admin_example/_delete_form.html.twig') }}
{% endblock %}
