<form method="post" action="{{ path('admin_meaning_delete', {'id': meaning.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meaning.id) }}">
    <button class="btn">Delete</button>
</form>
