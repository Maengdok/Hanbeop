<form method="post" action="{{ path('admin_letter_delete', {'id': letter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ letter.id) }}">
    <button class="btn">Delete</button>
</form>
